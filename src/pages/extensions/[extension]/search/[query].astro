---
import Layout from "../../../../layouts/Layout.astro";
// import PostByQuery from "/index";
import Search from "./search";
const { extension } = Astro.params;
---

<Layout title={String(extension) + " search"}>
  <Search extension={extension} client:load></Search>
</Layout>
