---
import Layout from "../../../layouts/Layout.astro";
import Extension from "./index";
const { extension } = Astro.params;
---

<Layout title={String(extension)}>
  <Extension client:load extension={extension}/>
</Layout>
